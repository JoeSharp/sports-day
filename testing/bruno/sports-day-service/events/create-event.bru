meta {
  name: create-event
  type: http
  seq: 1
}

post {
  url: {{serviceUrl}}/api/events
  body: json
  auth: inherit
}

body:json {
  {
    "activityId": "66f5683f-63ba-4cc6-ba13-c5e3b9a58c2d",
    "competitorType": "INDIVIDUAL",
    "scoreType": "FINISHING_ORDER",
    "maxParticipants": 4
  }
}

script:post-response {
  const eventId = res.getBody().id;
  bru.setVar('eventId', eventId);
}

settings {
  encodeUrl: true
}

meta {
  name: get-all-activities
  type: http
  seq: 1
}

get {
  url: {{serviceUrl}}/api/activities
  body: none
  auth: inherit
}

script:post-response {
  const firstActivityId = res.getBody()[0].id;
  bru.setVar('activityId', firstActivityId);
}

settings {
  encodeUrl: true
}

meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{serviceUrl}}/api/auth/login
  body: formUrlEncoded
  auth: inherit
}

body:form-urlencoded {
  username: {{username}}
  password: {{password}}
}

settings {
  encodeUrl: true
}
